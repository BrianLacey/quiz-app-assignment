import { db } from "../db";

const contentServices = {
  readByParams: () => {
    // return db().collection("content").find().toArray();
  },
  postScore: () => {},
};

export default contentServices;
